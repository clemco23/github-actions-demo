name: Hello World Workflow

on:
  push:   # le workflow se déclenche quand tu fais un push
    branches:
      - main

jobs:
  greeting:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello World
        run: echo "Hello World"
